// sampleWithoutErrors.js

const greet = (name) => {
    console.log(`Hello, ${name}`); // Template string used correctly
};

greet("World");

const anotherFunction = () => {
    // This function is used in the code, no errors here
    console.log("This function is useful.");
};

anotherFunction();
